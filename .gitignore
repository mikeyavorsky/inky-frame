# WiFi credentials - local to each device, never committed.
secrets.py

# OTA runtime state written on-device.
version.txt
bad_version.txt
update_pending
*.new
*.bak

# Inky Frame runtime state (which app to run, cached images).
state.json
nasa-apod-daily
